# PAPI Copy-Paste Environment Configuration Example

# Mock Metadata Feature Flag
# Set to "true" to enable mock metadata fallback when real metadata fails
# Set to "false" to disable mock metadata (app will show empty pallets when real metadata fails)
# Default: false
NEXT_PUBLIC_ENABLE_MOCK_METADATA=false